# v3.5 离线复算说明

本说明只定义字节与 canonical hash 的核验顺序，不执行代码、读取行情或提交外部 anchor。完整规则字段在绑定的 record 内；正确推荐定义来源及 hash 见 anchor JSON/README。

记录 hash 应覆盖 `24_SEED_A预注册记录_v3_5_20260920.json` 的完整原始 UTF-8 字节；当前 bytes=17941、SHA-256=`8773AA46E86992DA6D19A68877844D2601BB81B21712DA07CA371B10385B8394`。canonical hash 只覆盖该记录的 `canonical_payload`，当前 SHA-256=`19fbb3f43a842584547d6ff5495461a16e17b202CE6E8B2460C5B5A7FC846542`；不覆盖外层 record、anchor JSON 或本说明，因此不存在循环引用。

v3.5 的未来发布必须在独立核验后另行登记；本目录的存在不改变 v3.4 已发布事实，也不产生工程或研究读取许可。
