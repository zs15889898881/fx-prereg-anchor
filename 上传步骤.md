# 上传步骤（给有 GitHub 账号的人，一步一步来）

目标：把这个锚文件**公开保存**到项目之外，拿到一个**链接**发回给总控。

> 准备：本目录里共有 **3 个文件**。上传时**只上传这 3 个**，
> **绝对不要**把本项目的其它任何文件、行情数据、策略参数表、业绩数字放进去。

---

## 路线 (a) 新建一个专用的公开仓库（**推荐**）

**为什么推荐**：平台的“提交时间”由第三方记录，证据强度最高；而且仓库可以长期留着。

1. 打开 github.com，点右上角 **+** → **New repository**。
2. **Repository name** 填：`fx-prereg-anchor`。
3. **Description**（可选）填：`SEED_A preregistration freeze anchor (no market data)`。
4. **Visibility** 必须选 **Public**（公开）。**不要**选 Private。
5. **不要**勾选 “Add a README file”“Add .gitignore”“Choose a license”
   —— 我们要**空仓库**，这样里面只有我们上传的文件。
6. 点 **Create repository**。
7. 在新页面点 **uploading an existing file**（或 **Add file → Upload files**）。
8. 把本目录的 3 个文件**一起**拖进去：
   - `FX_SEED_A_PREREGISTRATION_RECORD_V3_20260919_01.anchor.json`
   - `README.md`
   - `上传步骤.md`
9. 页面下方 **Commit changes** 的说明可留默认（例如 `Add SEED_A preregistration v3 freeze anchor`），
   然后点 **Commit changes**。
10. 回到仓库首页，**复制地址栏的链接**（形如 `https://github.com/你的用户名/fx-prereg-anchor`），
    把它**发回给总控**。

**完成后请注意**：
- 以后**不要**再往这个仓库里放别的文件（保持它只装这份锚，证据最干净）。
- 如果你之后删掉或改名，**历史仍可能被缓存/镜像**；所以“删除”并不能消除它的存在，
  只能再补一条说明。

---

## 路线 (b) 用一个公开 Gist（**更简单，证据强度略低**）

1. 打开 gist.github.com（用同一个 GitHub 账号登录）。
2. **Filename** 依次填 3 个文件名（点 “Add file” 增加行）：
   - `FX_SEED_A_PREREGISTRATION_RECORD_V3_20260919_01.anchor.json`
   - `README.md`
   - `上传步骤.md`
3. 把每个文件的内容粘贴进去。
   **粘贴锚文件内容时不要多敲回车**（它是**没有尾随换行**的；多一个换行，校验码就变了）。
4. 如果页面有 “Create public gist / Public” 的选项，**必须选 Public**（公开）。
5. 点 **Create gist** 或 **Create public gist**。
6. **复制地址栏的链接**（形如 `https://gist.github.com/你的用户名/一长串字符`），发回给总控。

**与 (a) 的差别**：Gist 更省事，但**创建者可以随时修改或删除**，所以作为证据强度略低；
仓库（路线 a）因为留痕更完整，更不容易被质疑。

---

## 上传完成后总控会做什么

1. 把你发回的**链接**和**平台显示的提交/创建时间**记下来；
2. 把这两样登记为 `external_anchor` 的**发布证据**，把状态从
   `NOT_PUBLISHED_PENDING_OWNER_APPROVAL` 改为**已发布**；
3. 只有到那时，“口径变更生效”的第二个前置条件才算满足。

## 三条安全提醒

1. **只放本目录这 3 个文件**；不要放行情数据、策略参数、业绩数字、账号信息。
2. **不要**用 `Private`（那样别人核验不了，等于没有外部锚）。
3. 上传后**把链接发回总控**；不发回，我们这边无法登记，锚就等于没生效。
